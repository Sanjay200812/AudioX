# AudioX Vercel Environment Configuration
# ZERO environment variables are strictly required for normal deployments.
# AudioX runs 100% on Vercel with zero external dependencies, zero databases, and zero secrets.

# Optional: If your Vercel plan or deployment requires large bundle support for the bundled FFmpeg static binary:
# VERCEL_SUPPORT_LARGE_FUNCTIONS=1
